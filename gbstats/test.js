// const fs = require('fs');

const numbers = 3456000
const store = Array.from({length: numbers}, () => Math.floor(Math.random() * 4095));

// fs.writeFileSync('big.txt', JSON.stringify(store)); // 16,000 KB (15 MB)

while (true) {

}